
	
	let pc = document.querySelector('.PC');
let mobile = document.querySelector('.mobile');


const screenSize = window.innerWidth || document.body.clientWidth || document.documentElement.clientWidth;


//check the screen size. The pc version will use the complex version and the mobile screen will use the simple version.

function checkScreenSize (){
	
	if( screenSize < 600 ){
		
	
		
		
		pc.style.visibility = 'hidden';
		mobile.style.visibility = 'visible';
		
		
	} else {
		
pc.style.visibility = 'visible';
mobile.style.visibility = 'hidden';   
	}
	
	
}


checkScreenSize();
	
	let pc = document.querySelector('.PC');
let mobile = document.querySelector('.mobile');


const screenSize = window.innerWidth || document.body.clientWidth || document.documentElement.clientWidth;


//check the screen size. The pc version will use the complex version and the mobile screen will use the simple version.

function checkScreenSize (){
	
	if( screenSize < 600 ){
		
	
		
		
		pc.style.visibility = 'hidden';
		mobile.style.visibility = 'visible';
		
		
	} else {
		
pc.style.visibility = 'visible';
mobile.style.visibility = 'hidden';   
	}
	
	
}


checkScreenSize();
	
	let pc = document.querySelector('.PC');
let mobile = document.querySelector('.mobile');


const screenSize = window.innerWidth || document.body.clientWidth || document.documentElement.clientWidth;


//check the screen size. The pc version will use the complex version and the mobile screen will use the simple version.

function checkScreenSize (){
	
	if( screenSize < 600 ){
		
	
		
		
		pc.style.visibility = 'hidden';
		mobile.style.visibility = 'visible';
		
		
	} else {
		
pc.style.visibility = 'visible';
mobile.style.visibility = 'hidden';   
	}
	
	
}


checkScreenSize();
	
	let pc = document.querySelector('.PC');
let mobile = document.querySelector('.mobile');


const screenSize = window.innerWidth || document.body.clientWidth || document.documentElement.clientWidth;


//check the screen size. The pc version will use the complex version and the mobile screen will use the simple version.

function checkScreenSize (){
	
	if( screenSize < 600 ){
		
	
		
		
		pc.style.visibility = 'hidden';
		mobile.style.visibility = 'visible';
		
		
	} else {
		
pc.style.visibility = 'visible';
mobile.style.visibility = 'hidden';   
	}
	
	
}


checkScreenSize();
	
	let pc = document.querySelector('.PC');
let mobile = document.querySelector('.mobile');


const screenSize = window.innerWidth || document.body.clientWidth || document.documentElement.clientWidth;


//check the screen size. The pc version will use the complex version and the mobile screen will use the simple version.

function checkScreenSize (){
	
	if( screenSize < 600 ){
		
	
		
		
		pc.style.visibility = 'hidden';
		mobile.style.visibility = 'visible';
		
		
	} else {
		
pc.style.visibility = 'visible';
mobile.style.visibility = 'hidden';   
	}
	
	
}


checkScreenSize();
	
	let pc = document.querySelector('.PC');
let mobile = document.querySelector('.mobile');


const screenSize = window.innerWidth || document.body.clientWidth || document.documentElement.clientWidth;


//check the screen size. The pc version will use the complex version and the mobile screen will use the simple version.

function checkScreenSize (){
	
	if( screenSize < 600 ){
		
	
		
		
		pc.style.visibility = 'hidden';
		mobile.style.visibility = 'visible';
		
		
	} else {
		
pc.style.visibility = 'visible';
mobile.style.visibility = 'hidden';   
	}
	
	
}


checkScreenSize();
	
	let pc = document.querySelector('.PC');
let mobile = document.querySelector('.mobile');


const screenSize = window.innerWidth || document.body.clientWidth || document.documentElement.clientWidth;


//check the screen size. The pc version will use the complex version and the mobile screen will use the simple version.

function checkScreenSize (){
	
	if( screenSize < 600 ){
		
	
		
		
		pc.style.visibility = 'hidden';
		mobile.style.visibility = 'visible';
		
		
	} else {
		
pc.style.visibility = 'visible';
mobile.style.visibility = 'hidden';   
	}
	
	
}


checkScreenSize();
	
	let pc = document.querySelector('.PC');
let mobile = document.querySelector('.mobile');


const screenSize = window.innerWidth || document.body.clientWidth || document.documentElement.clientWidth;


//check the screen size. The pc version will use the complex version and the mobile screen will use the simple version.

function checkScreenSize (){
	
	if( screenSize < 600 ){
		
	
		
		
		pc.style.visibility = 'hidden';
		mobile.style.visibility = 'visible';
		
		
	} else {
		
pc.style.visibility = 'visible';
mobile.style.visibility = 'hidden';   
	}
	
	
}


checkScreenSize();
	
	let pc = document.querySelector('.PC');
let mobile = document.querySelector('.mobile');


const screenSize = window.innerWidth || document.body.clientWidth || document.documentElement.clientWidth;


//check the screen size. The pc version will use the complex version and the mobile screen will use the simple version.

function checkScreenSize (){
	
	if( screenSize < 600 ){
		
	
		
		
		pc.style.visibility = 'hidden';
		mobile.style.visibility = 'visible';
		
		
	} else {
		
pc.style.visibility = 'visible';
mobile.style.visibility = 'hidden';   
	}
	
	
}


checkScreenSize();
	
	let pc = document.querySelector('.PC');
let mobile = document.querySelector('.mobile');


const screenSize = window.innerWidth || document.body.clientWidth || document.documentElement.clientWidth;


//check the screen size. The pc version will use the complex version and the mobile screen will use the simple version.

function checkScreenSize (){
	
	if( screenSize < 600 ){
		
	
		
		
		pc.style.visibility = 'hidden';
		mobile.style.visibility = 'visible';
		
		
	} else {
		
pc.style.visibility = 'visible';
mobile.style.visibility = 'hidden';   
	}
	
	
}


checkScreenSize();let pc = document.querySelector('.PC');
let mobile = document.querySelector('.mobile');let pc = document.querySelector('.PC');
let mobile = document.querySelector('.mobile');let pc = document.querySelector('.PC');
let mobile = document.querySelector('.mobile');let pc = document.querySelector('.PC');
let mobile = document.querySelector('.mobile');let pc = document.querySelector('.PC');
let mobile = document.querySelector('.mobile');let pc = document.querySelector('.PC');
let mobile = document.querySelector('.mobile');
	
	
	
	
	let pc = document.querySelector('.PC');
let mobile = document.querySelector('.mobile');


const screenSize = window.innerWidth || document.body.clientWidth || document.documentElement.clientWidth;


//check the screen size. The pc version will use the complex version and the mobile screen will use the simple version.

function checkScreenSize (){
	
	if( screenSize < 600 ){
		
	
		
		
		pc.style.visibility = 'hidden';
		mobile.style.visibility = 'visible';
		
		
	} else {
		
pc.style.visibility = 'visible';
mobile.style.visibility = 'hidden';   
	}
	
	
}


checkScreenSize();
	
	
	
	
	
	
	
	let pc = document.querySelector('.PC');
let mobile = document.querySelector('.mobile');


const screenSize = window.innerWidth || document.body.clientWidth || document.documentElement.clientWidth;


//check the screen size. The pc version will use the complex version and the mobile screen will use the simple version.

function checkScreenSize (){
	
	if( screenSize < 600 ){
		
	
		
		
		pc.style.visibility = 'hidden';
		mobile.style.visibility = 'visible';
		
		
	} else {
		
pc.style.visibility = 'visible';
mobile.style.visibility = 'hidden';   
	}
	
	
}


checkScreenSize();
	
	
	
	
	
	
	
	let pc = document.querySelector('.PC');
let mobile = document.querySelector('.mobile');


const screenSize = window.innerWidth || document.body.clientWidth || document.documentElement.clientWidth;


//check the screen size. The pc version will use the complex version and the mobile screen will use the simple version.

function checkScreenSize (){
	
	if( screenSize < 600 ){
		
	
		
		
		pc.style.visibility = 'hidden';
		mobile.style.visibility = 'visible';
		
		
	} else {
		
pc.style.visibility = 'visible';
mobile.style.visibility = 'hidden';   
	}
	
	
}


checkScreenSize();
	
	
	
	
	
	
	
	let pc = document.querySelector('.PC');
let mobile = document.querySelector('.mobile');


const screenSize = window.innerWidth || document.body.clientWidth || document.documentElement.clientWidth;


//check the screen size. The pc version will use the complex version and the mobile screen will use the simple version.

function checkScreenSize (){
	
	if( screenSize < 600 ){
		
	
		
		
		pc.style.visibility = 'hidden';
		mobile.style.visibility = 'visible';
		
		
	} else {
		
pc.style.visibility = 'visible';
mobile.style.visibility = 'hidden';   
	}
	
	
}


checkScreenSize();
	
	
	
	
	
	
	
	let pc = document.querySelector('.PC');
let mobile = document.querySelector('.mobile');


const screenSize = window.innerWidth || document.body.clientWidth || document.documentElement.clientWidth;


//check the screen size. The pc version will use the complex version and the mobile screen will use the simple version.

function checkScreenSize (){
	
	if( screenSize < 600 ){
		
	
		
		
		pc.style.visibility = 'hidden';
		mobile.style.visibility = 'visible';
		
		
	} else {
		
pc.style.visibility = 'visible';
mobile.style.visibility = 'hidden';   
	}
	
	
}


checkScreenSize();
	
	
	
	
	
	
	
	let pc = document.querySelector('.PC');
let mobile = document.querySelector('.mobile');


const screenSize = window.innerWidth || document.body.clientWidth || document.documentElement.clientWidth;


//check the screen size. The pc version will use the complex version and the mobile screen will use the simple version.

function checkScreenSize (){
	
	if( screenSize < 600 ){
		
	
		
		
		pc.style.visibility = 'hidden';
		mobile.style.visibility = 'visible';
		
		
	} else {
		
pc.style.visibility = 'visible';
mobile.style.visibility = 'hidden';   
	}
	
	
}


checkScreenSize();
	
	
	
	
	
	
	
	let pc = document.querySelector('.PC');
let mobile = document.querySelector('.mobile');


const screenSize = window.innerWidth || document.body.clientWidth || document.documentElement.clientWidth;


//check the screen size. The pc version will use the complex version and the mobile screen will use the simple version.

function checkScreenSize (){
	
	if( screenSize < 600 ){
		
	
		
		
		pc.style.visibility = 'hidden';
		mobile.style.visibility = 'visible';
		
		
	} else {
		
pc.style.visibility = 'visible';
mobile.style.visibility = 'hidden';   
	}
	
	
}


checkScreenSize();
	
	
	
	
	
	
	
	let pc = document.querySelector('.PC');
let mobile = document.querySelector('.mobile');


const screenSize = window.innerWidth || document.body.clientWidth || document.documentElement.clientWidth;


//check the screen size. The pc version will use the complex version and the mobile screen will use the simple version.

function checkScreenSize (){
	
	if( screenSize < 600 ){
		
	
		
		
		pc.style.visibility = 'hidden';
		mobile.style.visibility = 'visible';
		
		
	} else {
		
pc.style.visibility = 'visible';
mobile.style.visibility = 'hidden';   
	}
	
	
}


checkScreenSize();
	
	
	
	
	
	
	
	let pc = document.querySelector('.PC');
let mobile = document.querySelector('.mobile');


const screenSize = window.innerWidth || document.body.clientWidth || document.documentElement.clientWidth;


//check the screen size. The pc version will use the complex version and the mobile screen will use the simple version.

function checkScreenSize (){
	
	if( screenSize < 600 ){
		
	
		
		
		pc.style.visibility = 'hidden';
		mobile.style.visibility = 'visible';
		
		
	} else {
		
pc.style.visibility = 'visible';
mobile.style.visibility = 'hidden';   
	}
	
	
}


checkScreenSize();
	
	
	
	
	
	
	
	let pc = document.querySelector('.PC');
let mobile = document.querySelector('.mobile');


const screenSize = window.innerWidth || document.body.clientWidth || document.documentElement.clientWidth;


//check the screen size. The pc version will use the complex version and the mobile screen will use the simple version.

function checkScreenSize (){
	
	if( screenSize < 600 ){
		
	
		
		
		pc.style.visibility = 'hidden';
		mobile.style.visibility = 'visible';
		
		
	} else {
		
pc.style.visibility = 'visible';
mobile.style.visibility = 'hidden';   
	}
	
	
}


checkScreenSize();
	
	
	
	
	
	let pc = document.querySelector('.PC');
let mobile = document.querySelector('.mobile');


const screenSize = window.innerWidth || document.body.clientWidth || document.documentElement.clientWidth;


//check the screen size. The pc version will use the complex version and the mobile screen will use the simple version.

function checkScreenSize (){
	
	if( screenSize < 600 ){
		
	
		
		
		pc.style.visibility = 'hidden';
		mobile.style.visibility = 'visible';
		
		
	} else {
		
pc.style.visibility = 'visible';
mobile.style.visibility = 'hidden';   
	}
	
	
}


checkScreenSize();
	
	
	
	
	
	let pc = document.querySelector('.PC');
let mobile = document.querySelector('.mobile');


const screenSize = window.innerWidth || document.body.clientWidth || document.documentElement.clientWidth;


//check the screen size. The pc version will use the complex version and the mobile screen will use the simple version.

function checkScreenSize (){
	
	if( screenSize < 600 ){
		
	
		
		
		pc.style.visibility = 'hidden';
		mobile.style.visibility = 'visible';
		
		
	} else {
		
pc.style.visibility = 'visible';
mobile.style.visibility = 'hidden';   
	}
	
	
}


checkScreenSize();
	
	
	
	
	
	let pc = document.querySelector('.PC');
let mobile = document.querySelector('.mobile');


const screenSize = window.innerWidth || document.body.clientWidth || document.documentElement.clientWidth;


//check the screen size. The pc version will use the complex version and the mobile screen will use the simple version.

function checkScreenSize (){
	
	if( screenSize < 600 ){
		
	
		
		
		pc.style.visibility = 'hidden';
		mobile.style.visibility = 'visible';
		
		
	} else {
		
pc.style.visibility = 'visible';
mobile.style.visibility = 'hidden';   
	}
	
	
}


checkScreenSize();
	
	
	
	
	let pc = document.querySelector('.PC');
let mobile = document.querySelector('.mobile');


const screenSize = window.innerWidth || document.body.clientWidth || document.documentElement.clientWidth;


//check the screen size. The pc version will use the complex version and the mobile screen will use the simple version.

function checkScreenSize (){
	
	if( screenSize < 600 ){
		
	
		
		
		pc.style.visibility = 'hidden';
		mobile.style.visibility = 'visible';
		
		
	} else {
		
pc.style.visibility = 'visible';
mobile.style.visibility = 'hidden';   
	}
	
	
}


checkScreenSize();
	
	let pc = document.querySelector('.PC');
let mobile = document.querySelector('.mobile');


const screenSize = window.innerWidth || document.body.clientWidth || document.documentElement.clientWidth;


//check the screen size. The pc version will use the complex version and the mobile screen will use the simple version.

function checkScreenSize (){
	
	if( screenSize < 600 ){
		
	
		
		
		pc.style.visibility = 'hidden';
		mobile.style.visibility = 'visible';
		
		
	} else {
		
pc.style.visibility = 'visible';
mobile.style.visibility = 'hidden';   
	}
	
	
}


checkScreenSize();// JavaScript Document


